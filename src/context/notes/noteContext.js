// importing create context api from react 
import { createContext } from "react";
// we are creating context 
const noteContext = createContext();

// mow we are exporting 
export default noteContext;